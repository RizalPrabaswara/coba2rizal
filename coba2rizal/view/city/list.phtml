<a href="<?= $this->url("/city/list", ["action" => "create"]) ?>">Add New City</a>

<table>
    <thead>
        <tr>
            <th>ID City</th>
            <th>City Name</th>
            <th>Country</th>
            <th>Action</th>
            <th>Jumlah Bangunan</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($cities as $city) : ?>
            <tr>
                <?= sprintf("<td>%s</td>", $city->id); ?>
                <?= sprintf("<td>%s</td>", $city->city_name); ?>
                <?= sprintf("<td>%s</td>", $city->getCountry()); ?>
                <td>
                    <ul class="row-action">
                        <?= sprintf("<li><a href=\"%s\">Edit</a></li>", $this->url(sprintf("/city/%d", $city->id))); ?>
                        <?= sprintf("<li><a href=\"%s\">Delete</a></li>", $this->url(sprintf("/city/%d", $city->id), ["delete" => 1])); ?>
                    </ul>
                </td>
            <?php endforeach; ?>
            <?= sprintf("<td>%s</td>", $counts); ?>
            </tr>
    </tbody>
</table>